{%- liquid
  assign key_ingredients = product.metafields.custom.key_ingredients.value
  assign gallery_size = product.images.size
-%}

<section class="product-key-ingredients full-width">
  <div class="product-key-ingredients__inner cham-container">
    <div class="product-key-ingredients__head" data-reveal>
      {%- if section.settings.eyebrow != blank -%}
        <span class="eyebrow">{{ section.settings.eyebrow }}</span>
      {%- endif -%}
      <h2 class="product-key-ingredients__heading font-display">
        {{- section.settings.heading_lead -}}
        {%- if section.settings.heading_emphasis != blank %} <em>{{ section.settings.heading_emphasis }}</em>{%- endif -%}
      </h2>
      {%- if section.settings.body != blank -%}
        <div class="product-key-ingredients__body">{{ section.settings.body }}</div>
      {%- endif -%}
    </div>

    {%- if key_ingredients != blank and key_ingredients.size > 0 -%}
      <div class="product-key-ingredients__grid">
        {%- for ingredient in key_ingredients -%}
          {%- liquid
            assign card_image = ingredient.image
            if card_image == blank and gallery_size > 0
              assign cycle_idx = forloop.index | modulo: gallery_size
              assign card_image = product.images[cycle_idx]
            endif
          -%}
          <article class="product-key-ingredients__card" data-reveal data-reveal-delay="{{ forloop.index0 | times: 100 }}">
            <div class="product-key-ingredients__media">
              {%- if card_image != blank -%}
                <img
                  src="{{ card_image | image_url: width: 900 }}"
                  srcset="{{ card_image | image_url: width: 450 }} 450w, {{ card_image | image_url: width: 900 }} 900w, {{ card_image | image_url: width: 1400 }} 1400w"
                  sizes="(min-width: 768px) 33vw, 100vw"
                  alt="{{ ingredient.name | escape }}"
                  width="{{ card_image.width | default: 900 }}"
                  height="{{ card_image.height | default: 1100 }}"
                  loading="lazy"
                  decoding="async"
                  class="product-key-ingredients__img"
                >
              {%- else -%}
                {{ 'product-1' | placeholder_svg_tag: 'product-key-ingredients__img product-key-ingredients__img--placeholder' }}
              {%- endif -%}
              <h3 class="product-key-ingredients__name font-display">{{ ingredient.name }}</h3>
            </div>
            <p class="product-key-ingredients__role">{{ ingredient.role }}</p>
          </article>
        {%- endfor -%}
      </div>
    {%- endif -%}
  </div>
</section>

{% stylesheet %}
  .product-key-ingredients {
    width: 100%;
    padding-block: 7rem;
  }

  @media (min-width: 1024px) {
    .product-key-ingredients {
      padding-block: 11rem;
    }
  }

  .product-key-ingredients__head {
    max-width: 60rem;
  }

  .product-key-ingredients__heading {
    margin-top: 1.75rem;
    font-size: clamp(2.75rem, 6.5vw, 5.5rem);
    font-weight: 300;
    line-height: 1.0;
    letter-spacing: -0.015em;
  }

  .product-key-ingredients__heading em {
    color: var(--primary);
    font-style: italic;
  }

  .product-key-ingredients__body {
    margin-top: 1.75rem;
    max-width: 38rem;
    color: color-mix(in oklab, var(--foreground) 75%, transparent);
    line-height: 1.7;
    font-size: 1.0625rem;
  }

  .product-key-ingredients__body > p {
    margin: 0;
  }

  /* La Mer: tight, edge-to-edge image grid with minimal gap. */
  .product-key-ingredients__grid {
    margin-top: 4rem;
    display: grid;
    grid-template-columns: 1fr;
    gap: 0.5rem;
  }

  @media (min-width: 768px) {
    .product-key-ingredients__grid {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }
  }

  @media (min-width: 1024px) {
    .product-key-ingredients__grid {
      margin-top: 5.5rem;
    }
  }

  /* Image with the ingredient name overlaid bottom-left, La Mer style. */
  .product-key-ingredients__media {
    position: relative;
    aspect-ratio: 4 / 5;
    overflow: hidden;
    background-color: var(--surface);
  }

  .product-key-ingredients__img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .product-key-ingredients__img--placeholder {
    color: color-mix(in oklab, var(--primary) 30%, var(--surface));
  }

  .product-key-ingredients__name {
    position: absolute;
    left: 1.5rem;
    bottom: 1.5rem;
    right: 1.5rem;
    color: #fff;
    font-size: 1.75rem;
    font-weight: 300;
    line-height: 1.1;
    text-shadow: 0 1px 18px rgb(0 0 0 / 0.35);
  }

  @media (min-width: 1024px) {
    .product-key-ingredients__name {
      font-size: 2rem;
      left: 2rem;
      bottom: 2rem;
    }
  }

  .product-key-ingredients__role {
    margin-top: 1.25rem;
    padding-inline: 0.25rem;
    color: color-mix(in oklab, var(--foreground) 75%, transparent);
    line-height: 1.7;
  }
{% endstylesheet %}

{% schema %}
{
  "name": "t:sections.product_key_ingredients.name",
  "tag": "section",
  "settings": [
    {
      "type": "text",
      "id": "eyebrow",
      "label": "t:labels.eyebrow",
      "default": "Key Ingredients"
    },
    {
      "type": "text",
      "id": "heading_lead",
      "label": "t:labels.heading_lead",
      "default": "A short, considered list —"
    },
    {
      "type": "text",
      "id": "heading_emphasis",
      "label": "t:labels.heading_emphasis",
      "default": "no single hero."
    },
    {
      "type": "richtext",
      "id": "body",
      "label": "t:labels.body",
      "default": "<p>Skin response depends on how ingredients interact within a formulation. We focus on synergy, base quality and stability — never isolating one ingredient at the expense of the system.</p>"
    }
  ],
  "presets": [
    {
      "name": "t:sections.product_key_ingredients.presets_default"
    }
  ],
  "enabled_on": {
    "templates": ["product"]
  }
}
{% endschema %}
